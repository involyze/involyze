{
  "name": "involyze",
  "private": true,
  "workspaces": [
    "functions/*",
    "infrastructure",
    "involyze-app"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "bootstrap": "dotenv -e .env -- sh -c 'npm run cdk bootstrap aws://$ACCOUNT_ID/$AWS_REGION --workspace infrastructure'",
    "deploy": "dotenv -e .env -- npm run cdk deploy --workspace infrastructure",
    "postprocessing": "dotenv -e .env -- npm run postprocessing --workspace infrastructure",
    "pipeline": "npm install && npm run build && npm run deploy && npm run postprocessing"
  },
  "devDependencies": {
    "dotenv-cli": "^8.0.0",
    "ncp": "^2.0.0"
  }
}